<div class="container">
  <div class="row">
    <h3 class="text-center">Contact Us</h3>
    <p class="text-center col-md-4 col-md-offset-4">Sharestone would love to hear your feedback! However, since I am a one man team at the moment, please understand that you may not get a response immediately. Thank you!</p>
    <div class="col-md-4 col-md-offset-4">
      <%= flash[:error] %>
      <div class="well">
        <%= form_for @contact do |f| %>
          <div class="form-group">
            <%= f.label :name %>
            <%= f.text_field :name, class: 'form-control' %>
          </div>
          <div class="form-group">
            <%= f.label :email %>
            <%= f.text_field :email, class: 'form-control' %>
          </div>
          <div class="form-group">
            <%= f.label :comments %>
            <%= f.text_area :comments, class: 'form-control' %>
          </div>
          <%= f.submit 'Submit', class: 'btn btn-default' %>
        <% end %>
      </div>
    </div>
  </div>
</div>